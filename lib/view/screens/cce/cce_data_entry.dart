import 'package:flutter/material.dart';

class CceDataEntry extends StatelessWidget {
  const CceDataEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
