import 'package:flutter/material.dart';

class YieldCCEView extends StatelessWidget {
  const YieldCCEView({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(12),
      child: const Center(
        child: Text('Yield CCE Data Entry'),
      ),
    );
  }
}